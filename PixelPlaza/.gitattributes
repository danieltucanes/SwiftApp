# Asegura que todos los archivos de texto usen LF
* text=auto eol=lf

# Evita que las imágenes se vean afectadas
*.png binary
*.jpg binary
*.jpeg binary

# Si tienes archivos de código específicos como Swift
*.swift text eol=lf

# Otros archivos de texto como JSON, XML, etc.
*.json text eol=lf
*.xml text eol=lf
